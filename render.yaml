services:
  - type: web
    name: aeo-geo-analysis-section
    runtime: node
    rootDir: server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TRUST_PROXY_HOPS
        sync: false
